# Donor Board — Admin Theme (Right Column)

- Restores the **Theme panel** on the **admin page** (right sticky column).
- Admin can push theme updates to the display (BroadcastChannel) and they persist in localStorage.
- Left column retains **textbox input**, **CSV import + threshold emphasize**, and **chips**.

Open `admin.html` and `display.html` via a local server (e.g., `python3 -m http.server`).